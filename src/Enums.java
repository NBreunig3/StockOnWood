public class Enums {
    public enum OrderBuyOrSell {
        BUY,
        SELL
    }

    public enum OrderType {
        MARKET,
        LIMIT,
        STOPLOSS,
        BUYSTOP
    }

    public enum OrderStatus {
        PENDING,
        COMPLETED
    }
}
